# Articles Assistant Environment Configuration

# ===== Core Configuration =====
NODE_ENV=development
PORT=3002

# ===== OpenAI Configuration =====
# Required: Your OpenAI API key for GPT-4 and embeddings
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===== Database Configuration =====
# PostgreSQL with pgvector extension
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=articles-assistant
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Optional: Full database URL (overrides individual settings above)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/articles-assistant

# Container name for Docker deployment
DB_CONTAINER_NAME=articles-assistant-db

# ===== Cache Configuration =====
# Redis configuration
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379
REDIS_CONTAINER_NAME=articles-assistant-redis

# ===== API Configuration =====
# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002

# Rate limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=20

# ===== Content Ingestion Configuration =====
# Optional: Filter content by series (comma-separated)
# TARGET_SERIES=from-senior-to-senior

# Optional: Exclude specific series (comma-separated)
# EXCLUDE_SERIES=deprecated-series

# Optional: Filter by content status (comma-separated)
# ONLY_STATUS=published,draft

# Optional: Disable series filtering
# DISABLE_SERIES_FILTER=true

# Optional: File pattern regex for content filtering
# FILE_PATTERN=.*\.en\.mdx$